# Flask + Express To‑Do (Dockerized)


Frontend: Node.js + Express serving a form → posts to Flask backend.
Backend: Flask with CORS enabled.
Docker: Separate images, compose for local dev.


## Run (Docker)
```sh
docker compose up --build -d
